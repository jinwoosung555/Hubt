-- Ken Hub All-In-One Script (Key System + Rayfield Custom UI + Aimbot + Boosts + Credits)
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local Camera = workspace.CurrentCamera

-- Configuration Variables
local Config = {
    PlayerAimbot = false,
    NPCAimbot = false,
    SpeedBoost = false,
    SpeedValue = 25,
    JumpBoost = false,
    JumpValue = 75,
    WebsiteKeyURL = "https://jinwoosung555.github.io/Keyy/"
}

-- 1. Key System Gate UI
local KeySystemGUI = Instance.new("ScreenGui")
KeySystemGUI.Name = "KenHubKeySystem"
KeySystemGUI.Parent = game.CoreGui

local KeyFrame = Instance.new("Frame")
KeyFrame.Parent = KeySystemGUI
KeyFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 18)
KeyFrame.Position = UDim2.new(0.5, -190, 0.5, -125)
KeyFrame.Size = UDim2.new(0, 380, 0, 250)
Instance.new("UICorner", KeyFrame).CornerRadius = UDim.new(0, 8)

local KeyTitle = Instance.new("TextLabel")
KeyTitle.Parent = KeyFrame
KeyTitle.BackgroundTransparency = 1
KeyTitle.Position = UDim2.new(0, 0, 0, 15)
KeyTitle.Size = UDim2.new(1, 0, 0, 30)
KeyTitle.Font = Enum.Font.GothamBold
KeyTitle.Text = "Ken Hub - Key Verification"
KeyTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
KeyTitle.TextSize = 16

local KeyInfo = Instance.new("TextLabel")
KeyInfo.Parent = KeyFrame
KeyInfo.BackgroundTransparency = 1
KeyInfo.Position = UDim2.new(0, 15, 0, 45)
KeyInfo.Size = UDim2.new(1, -30, 0, 35)
KeyInfo.Font = Enum.Font.Gotham
KeyInfo.Text = "Visit website to join Discord & claim your 24hr key."
KeyInfo.TextColor3 = Color3.fromRGB(150, 150, 150)
KeyInfo.TextSize = 12
KeyInfo.TextWrapped = true

local WebBtn = Instance.new("TextButton")
WebBtn.Parent = KeyFrame
WebBtn.BackgroundColor3 = Color3.fromRGB(35, 35, 45)
WebBtn.Position = UDim2.new(0.1, 0, 0, 85)
WebBtn.Size = UDim2.new(0.8, 0, 0, 32)
WebBtn.Font = Enum.Font.GothamMedium
WebBtn.Text = "Copy Website Link"
WebBtn.TextColor3 = Color3.fromRGB(138, 43, 226)
WebBtn.TextSize = 13
Instance.new("UICorner", WebBtn).CornerRadius = UDim.new(0, 6)

WebBtn.MouseButton1Click:Connect(function()
    if setclipboard then
        setclipboard(Config.WebsiteKeyURL)
        WebBtn.Text = "Link Copied to Clipboard!"
        task.wait(2)
        WebBtn.Text = "Copy Website Link"
    else
        WebBtn.Text = Config.WebsiteKeyURL
    end
end)

local TextBox = Instance.new("TextBox")
TextBox.Parent = KeyFrame
TextBox.BackgroundColor3 = Color3.fromRGB(22, 22, 26)
TextBox.Position = UDim2.new(0.1, 0, 0, 125)
TextBox.Size = UDim2.new(0.8, 0, 0, 35)
TextBox.Font = Enum.Font.Gotham
TextBox.PlaceholderText = "Paste your 24hr key here..."
TextBox.Text = ""
TextBox.TextColor3 = Color3.fromRGB(255, 255, 255)
TextBox.TextSize = 14
Instance.new("UICorner", TextBox).CornerRadius = UDim.new(0, 6)

local SubmitBtn = Instance.new("TextButton")
SubmitBtn.Parent = KeyFrame
SubmitBtn.BackgroundColor3 = Color3.fromRGB(88, 101, 242)
SubmitBtn.Position = UDim2.new(0.1, 0, 0, 175)
SubmitBtn.Size = UDim2.new(0.8, 0, 0, 40)
SubmitBtn.Font = Enum.Font.GothamBold
SubmitBtn.Text = "Verify Key"
SubmitBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
SubmitBtn.TextSize = 14
Instance.new("UICorner", SubmitBtn).CornerRadius = UDim.new(0, 6)

local verified = false

SubmitBtn.MouseButton1Click:Connect(function()
    if string.sub(TextBox.Text, 1, 7) == "KENHUB-" and string.sub(TextBox.Text, -4) == "-24H" then
        verified = true
        KeySystemGUI:Destroy()
        LoadMainUI()
    else
        SubmitBtn.Text = "Invalid Key! Try Again."
        task.wait(1.5)
        SubmitBtn.Text = "Verify Key"
    end
end)

-- 2. Main Hub UI Generation (Executed after Key Pass)
function LoadMainUI()
    local KenHubGUI = Instance.new("ScreenGui")
    KenHubGUI.Name = "KenHubGUI"
    KenHubGUI.Parent = game.CoreGui
    KenHubGUI.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

    local MainFrame = Instance.new("Frame")
    MainFrame.Name = "MainFrame"
    MainFrame.Parent = KenHubGUI
    MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 18)
    MainFrame.BorderSizePixel = 0
    MainFrame.Position = UDim2.new(0.5, -250, 0.5, -175)
    MainFrame.Size = UDim2.new(0, 500, 0, 350)
    Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 8)

    local TopBar = Instance.new("Frame")
    TopBar.Parent = MainFrame
    TopBar.BackgroundColor3 = Color3.fromRGB(22, 22, 26)
    TopBar.BorderSizePixel = 0
    TopBar.Size = UDim2.new(0, 500, 0, 40)
    Instance.new("UICorner", TopBar).CornerRadius = UDim.new(0, 8)

    local Title = Instance.new("TextLabel")
    Title.Parent = TopBar
    Title.BackgroundTransparency = 1
    Title.Position = UDim2.new(0, 15, 0, 0)
    Title.Size = UDim2.new(0, 200, 1, 0)
    Title.Font = Enum.Font.GothamBold
    Title.Text = "Ken Hub"
    Title.TextColor3 = Color3.fromRGB(255, 255, 255)
    Title.TextSize = 16
    Title.TextXAlignment = Enum.TextXAlignment.Left

    local Sidebar = Instance.new("Frame")
    Sidebar.Parent = MainFrame
    Sidebar.BackgroundTransparency = 1
    Sidebar.Position = UDim2.new(0, 0, 0, 45)
    Sidebar.Size = UDim2.new(0, 130, 1, -45)

    local ContentArea = Instance.new("Frame")
    ContentArea.Parent = MainFrame
    ContentArea.BackgroundTransparency = 1
    ContentArea.Position = UDim2.new(0, 140, 0, 45)
    ContentArea.Size = UDim2.new(0, 350, 1, -55)

    local function CreateTabButton(name, yPos)
        local btn = Instance.new("TextButton")
        btn.Parent = Sidebar
        btn.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
        btn.BorderSizePixel = 0
        btn.Position = UDim2.new(0, 10, 0, yPos)
        btn.Size = UDim2.new(0, 110, 0, 30)
        btn.Font = Enum.Font.GothamMedium
        btn.Text = name
        btn.TextColor3 = Color3.fromRGB(200, 200, 200)
        btn.TextSize = 13
        Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 6)
        return btn
    end

    local function CreateTabContent()
        local frame = Instance.new("ScrollingFrame")
        frame.Parent = ContentArea
        frame.BackgroundTransparency = 1
        frame.Size = UDim2.new(1, 0, 1, 0)
        frame.CanvasSize = UDim2.new(0, 0, 0, 400)
        frame.ScrollBarThickness = 4
        frame.Visible = false
        return frame
    end

    local AimTabBtn = CreateTabButton("Combat", 10)
    local PlayerTabBtn = CreateTabButton("Player Mods", 50)
    local CreditsTabBtn = CreateTabButton("Credits", 90)

    local AimTab = CreateTabContent()
    local PlayerTab = CreateTabContent()
    local CreditsTab = CreateTabContent()
    AimTab.Visible = true

    AimTabBtn.MouseButton1Click:Connect(function()
        AimTab.Visible = true; PlayerTab.Visible = false; CreditsTab.Visible = false
    end)
    PlayerTabBtn.MouseButton1Click:Connect(function()
        AimTab.Visible = false; PlayerTab.Visible = true; CreditsTab.Visible = false
    end)
    CreditsTabBtn.MouseButton1Click:Connect(function()
        AimTab.Visible = false; PlayerTab.Visible = false; CreditsTab.Visible = true
    end)

    local function CreateToggle(parent, text, callback, y)
        local btn = Instance.new("TextButton")
        btn.Parent = parent
        btn.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
        btn.Position = UDim2.new(0, 0, 0, y)
        btn.Size = UDim2.new(0, 340, 0, 35)
        btn.Font = Enum.Font.GothamMedium
        btn.Text = "  " .. text .. ": [OFF]"
        btn.TextColor3 = Color3.fromRGB(200, 200, 200)
        btn.TextSize = 13
        btn.TextXAlignment = Enum.TextXAlignment.Left
        Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 6)
        
        local state = false
        btn.MouseButton1Click:Connect(function()
            state = not state
            btn.Text = "  " .. text .. ": " .. (state and "[ON]" or "[OFF]")
            callback(state)
        end)
    end

    CreateToggle(AimTab, "Player Aimbot", function(state) Config.PlayerAimbot = state end, 10)
    CreateToggle(AimTab, "NPC Aimbot", function(state) Config.NPCAimbot = state end, 55)

    CreateToggle(PlayerTab, "Speed Boost", function(state) Config.SpeedBoost = state end, 10)
    CreateToggle(PlayerTab, "Jump Boost", function(state) Config.JumpBoost = state end, 55)

    local function CreateCreditCard(parent, name, role, y)
        local card = Instance.new("Frame")
        card.Parent = parent
        card.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
        card.Position = UDim2.new(0, 0, 0, y)
        card.Size = UDim2.new(0, 340, 0, 50)
        Instance.new("UICorner", card).CornerRadius = UDim.new(0, 6)
        
        local n = Instance.new("TextLabel")
        n.Parent = card
        n.BackgroundTransparency = 1
        n.Position = UDim2.new(0, 15, 0, 5)
        n.Size = UDim2.new(0, 300, 0, 20)
        n.Font = Enum.Font.GothamBold
        n.Text = name
        n.TextColor3 = Color3.fromRGB(255, 255, 255)
        n.TextSize = 14
        n.TextXAlignment = Enum.TextXAlignment.Left
        
        local r = Instance.new("TextLabel")
        r.Parent = card
        r.BackgroundTransparency = 1
        r.Position = UDim2.new(0, 15, 0, 25)
        r.Size = UDim2.new(0, 300, 0, 20)
        r.Font = Enum.Font.Gotham
        r.Text = role
        r.TextColor3 = Color3.fromRGB(150, 150, 150)
        r.TextSize, r.TextXAlignment = 12, Enum.TextXAlignment.Left
    end

    CreateCreditCard(CreditsTab, "Jinwoo", "Main Scripter", 10)
    CreateCreditCard(CreditsTab, "KENzoro", "Contributor", 70)
    CreateCreditCard(CreditsTab, "Ayla", "Contributor", 130)
end

-- 3. Core Engine Loop (Aimbot & Character Tweaks)
RunService.RenderStepped:Connect(function()
    if not verified then return end

    if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
        local humanoid = LocalPlayer.Character.Humanoid
        if Config.SpeedBoost then humanoid.WalkSpeed = Config.SpeedValue end
        if Config.JumpBoost then humanoid.JumpPower = Config.JumpValue end
    end

    local target = nil
    local shortestDist = math.huge
    local mouse = LocalPlayer:GetMouse()
    
    if Config.PlayerAimbot then
        for _, player in ipairs(Players:GetPlayers()) do
            if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("Head") then
                local char = player.Character
                local humanoid = char:FindFirstChild("Humanoid")
                if humanoid and humanoid.Health > 0 then
                    local headPos, onScreen = Camera:WorldToViewportPoint(char.Head.Position)
                    if onScreen then
                        local dist = (Vector2.new(mouse.X, mouse.Y) - Vector2.new(headPos.X, headPos.Y)).Magnitude
                        if dist < shortestDist then
                            shortestDist = dist
                            target = char.Head
                        end
                    end
                end
            end
        end
    elseif Config.NPCAimbot then
        for _, obj in ipairs(workspace:GetDescendants()) do
            if obj:IsA("Model") and obj:FindFirstChild("Humanoid") and obj:FindFirstChild("Head") then
                if not Players:GetPlayerFromCharacter(obj) and obj.Humanoid.Health > 0 then
                    local headPos, onScreen = Camera:WorldToViewportPoint(obj.Head.Position)
                    if onScreen then
                        local dist = (Vector2.new(mouse.X, mouse.Y) - Vector2.new(headPos.X, headPos.Y)).Magnitude
                        if dist < shortestDist then
                            shortestDist = dist
                            target = obj.Head
                        end
                    end
                end
            end
        end
    end

    if target then
        Camera.CFrame = CFrame.new(Camera.CFrame.Position, target.Position)
    end
end)
